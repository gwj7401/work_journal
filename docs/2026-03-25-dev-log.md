# 开发日志

## 2026-03-25

- 启动本地开发环境
- 更新后端 `.env` 文件，将数据库名配置为本地 MySQL 数据库 `nxtj_db`，符合系统全局规范。
- 初始化 `nxtj_db` 数据库，并设置 `utf8mb4` 字符集确保中文注释不乱码。
- 计划分别启动后端 FastAPI 服务和前端 Vue3 服务进行测试。
- 修复了前后端接口联调时由于 8090 端口代理不匹配产生的 `502` 及在后端 `auth.py` 中新旧库产生的被 `passlib` 越界中断的密码 `500` 错误。
- 新增 PDF 转 Word 实用工具模块：
  - **后端**：引入 `pdf2docx` 库建立 `/api/tools/pdf-to-word` 独立后端服务路由（配置无残留的 BackgroundTask 文件防泄露销毁机制）。
  - **前端**：编写带完整拖板体验及加载逻辑的 `ToolsView.vue` 界面组件，并配置原生的响应二进制 Blob 文件流以保证通过 `el-upload` 可直接激活浏览器原生下载交互。
  -  在菜单布局体系 (`AppLayout`) 加入侧边栏的专属路径。
- **全面跨终端移动适配**：重写了 `AppLayout.vue` 主工作框架代码并融入媒体查询与抽屉拉门交互逻辑，彻底支持了手机小屏场景的展示。
- **工作日志编辑器质变增强**：
  - 于后方部署搭载了图片实体防污染回传机制的 `/api/upload/image` 专属接口，基于 `StaticFiles` 向内网派发静态媒体；
  - 彻底开放释放了前端 `md-editor-v3` 的高级潜能组件，全盘打通了剪贴板图片“一键 Ctrl+V” 直接上云渲染核心管道，实现了真正意义的图文混排原生享受。
